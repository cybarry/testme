import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Login - CBT Platform',
  description: 'Sign in to the Computer-Based Test platform'
};

export default function LoginPage() {
  return <LoginForm />;
}
