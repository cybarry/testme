import { RegisterForm } from '@/components/auth/register-form';

export const metadata = {
  title: 'Register - CBT Platform',
  description: 'Create a new account on the CBT platform'
};

export default function RegisterPage() {
  return <RegisterForm />;
}
